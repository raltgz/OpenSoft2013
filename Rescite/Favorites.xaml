<Page x:Class="Citeseer.FavouritesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
	Title="Favorites">

    <Grid x:Name="favorites">
        <StackPanel MinWidth="200">
            <Grid VerticalAlignment="Top" Margin="0,0,0,0" MaxWidth="1200" Height="20" Background="White" HorizontalAlignment="Stretch">
                <Button x:Name="btnClear" Content="Clear Favorites" Click="clear" HorizontalAlignment="Right" Background="White"/>
            </Grid>
            <Grid HorizontalAlignment="Stretch" x:Name="favoritesScroll" Margin="0,0,0,0" MaxHeight="700" MinWidth="850" >
                <ListBox x:Name = "favoritesPane" BorderBrush="LightGray" ScrollViewer.CanContentScroll="True" ScrollViewer.PanningDeceleration="0" ScrollViewer.IsDeferredScrollingEnabled="False" ScrollViewer.VerticalScrollBarVisibility="Auto" MaxWidth="1200" MaxHeight="700" MinHeight="560" />
            </Grid>
        </StackPanel>
    </Grid>
</Page>
